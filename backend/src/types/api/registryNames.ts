/** Canonical registry identifiers — shared between backend + frontend via @api. */

/** Action-substitution variables (referenced by automation payloads as `{{VAR}}`). */
export const ACTION_VARIABLES_REGISTRY = 'action-variables';

/** Per-variable metadata (description, editable/readonly, owner). */
export const ACTION_VARIABLES_META_REGISTRY = 'action-variables-meta';
